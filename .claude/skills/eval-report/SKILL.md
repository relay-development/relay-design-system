---
name: eval-report
description: evals の結果と行動ログから HTML レポートを生成し、所見つきで報告する。週次フル実行のあと・FAIL の調査時・DS 改善の前後比較時、または「評価レポートを出して」と言われたときに使う。レポート生成自体は LLM 不使用・無料。
---

# eval-report — evals の HTML レポート生成と読み方

## 手順

1. **生成**: `npm run eval:report:html` → `evals/results/report.html`（gitignored。決定的生成なので何度でも作り直してよい）
   - `-- --stamp <接頭辞>` で過去の実行を指定（例: `--stamp 2026-08-25T06-18`）
   - `-- --case <id>` でお題を絞り込み
   - `-- --all` で履歴マトリクスを全件表示（既定 20 件）
2. **読む順番**:
   - 見出しの R/C 集計（regression は 100% 維持が前提、capability は改善メーター）
   - 履歴推移マトリクスで**変化した列**（✓→✗ / ✗→✓）を探す。列は kind で帯分けされている
     （緑帯 = regression: ✗ は即対応 / 琥珀帯 = capability: ✗ は改善余地の測定値）。◀ が対象実行
   - 最新実行のお題別カード — FAIL したトライアルは自動で展開されている
3. **所見をユーザーに報告する**。形式は自由だが、次を必ず含める:
   - 変化（✓→✗ / ✗→✓ / 変化なし）を先頭に
   - fail は 3 方向で切り分ける — **知識 / 採点基準 / お題**。分水嶺は「正しい知識を持った理想のエージェントなら安定して合格できるか」（Yes なら知識か基準、No ならお題）。最終判断はユーザーに委ねる
   - **✗→✓ の改善も鵜呑みにしない** — 行動ログでメカニズム（どの経路で知識に到達したか）を確認してから改善と報告する（見かけの改善の除外）
   - ✓→✗ は劣化と断定する前に `--case <id> --trials 2` での再確認を提案（生成ブレの可能性）

## シグナルの扱い（レポート下部の「自動検出」）

レポートは次の候補を機械的に検出して表示する — 空振り（search の応答が小さい/ヒットなし）・
巨大応答（1 件で全応答の 35% 超）・突合の ✗（引いたのに未使用。過去の実例: button を引いた上で
当時のルールに従い link を選んでいた）・前回からの変化。

**検出はあくまで候補**。所見では各シグナルを 3 方向で切り分けて報告する。深掘りには
生ログ `evals/results/outputs/<実行スタンプ>/<id>.transcript.jsonl` を読む — エージェントの
発話も証拠になるが、必ずツール呼び出しの事実で裏取りする

## 共有

- ユーザーに求められたら、report.html を Artifact として公開する（favicon は 📊 で固定。同じファイルパスで再公開すれば URL は維持される）
- レポートは自動生成の事実のみ。所見・切り分けはチャットで添える（レポートに手書きで混ぜない）

## 注意

- このスキルが実行するのはレポート**生成**のみ（無料）。eval の**実行**（`npm run eval`）は LLM を消費するため、必ずユーザーに確認してから
- レポートの実装は [evals/report-html.mjs](../../../evals/report-html.mjs)。表示項目を足すときはそちらを編集（transcript 解析は evals/transcript.mjs を共用）
